# Production origin for canonical URLs, Open Graph, sitemap.xml and structured data.
# On Vercel the production domain is used automatically when this is unset.
NEXT_PUBLIC_SITE_URL=https://www.example.com
